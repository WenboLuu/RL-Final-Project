env_name: 'ALE/Alien-v5'
num_episodes: 50000
num_envs: 16
batch_size: 32
learning_rate: 0.00025
gamma: 0.99
target_update_freq: 4800
memory_size: 200000
epsilon_start: 1.0
epsilon_end: 0.01
epsilon_decay: 1.0e-5  # Ensure this is interpreted as a float
frame_skip: 4
eval_interval: 20000
num_eval_episodes: 10
eps_eval: 0.05
input_mode: 'grayscale'  # Use 'rgb' to process RGB frames, 'grayscale' to process grayscale frames